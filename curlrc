# Change default UA to Win10+FF
# Override with `-A` or `--user-agent`.
user-agent = "Mozilla/5.0 (Windows NT 10.0; WOW64; rv:75.0.0) Gecko/20100101 Firefox/75.0"

# Add prev URL as refered when following redirects
referer = ";auto"

# Set timeout
connect-timeout = 60
